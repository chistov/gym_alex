# В dev-режиме не нужен — работает через Vite proxy
# На Vercel тоже не нужен — API живёт на том же домене (/api/*)
# Указывайте только если бэкенд на отдельном сервере
# VITE_API_URL=https://your-backend.example.com/api
